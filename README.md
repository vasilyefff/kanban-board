# 🗂️ Kanban Board — Task Management App

Веб-приложение для управления задачами в формате Kanban, построенное на React.  
Позволяет создавать задачи, редактировать их и перемещать между колонками с помощью drag & drop, с автоматическим сохранением данных в браузере.

Основной целью проекта было построить приложение с разделением бизнес-логики и интерфейса с использованием кастомных хуков и масштабируемой структуры.

Проект создан как портфолио-приложение с акцентом на чистую архитектуру, управление состоянием и разделение бизнес-логики и UI.


## ✨ Основные возможности

- ➕ **Создание задач** — быстрое добавление новых карточек  
- ✏️ **Редактирование** — изменение названия задачи  
- 🗑️ **Удаление** — удаление задач из доски  
- 🔄 **Drag & Drop** — перемещение задач между колонками  
- 📌 **Управление статусами** — смена стадии выполнения  
- 💾 **Персистентное состояние** — автоматическое сохранение в localStorage  
- 🧠 **Чистая архитектура** — бизнес-логика вынесена в кастомные хуки  

---

## 🛠️ Технологический стек

- **React**
- **JavaScript**
- **CSS**
- **React Hooks** (useState, useEffect)
- **Custom Hooks** (`useBoard`, `useLocalStorage`)
- **Browser localStorage API**

Drag & drop реализован вручную, без сторонних библиотек.

---

## 📦 Установка

### Предварительные требования

- Node.js (18+)
- npm

### Шаги установки

1. Клонируйте репозиторий:

```bash
git clone https://github.com/vasilyefff/kanban-board.git
cd kanban-board
```

2. Установите зависимости:

```bash
npm install
```

3. Запустите dev-сервер:

```bash
npm run dev
```

---

## 🎯 Как использовать

1. **Добавление задачи** — введите название и нажмите кнопку добавления  
2. **Редактирование** — измените текст задачи  
3. **Перемещение** — перетащите карточку в другую колонку  
4. **Удаление** — удалите задачу кнопкой 🗑️  
5. Все изменения сохраняются автоматически  

---

## 📁 Структура проекта

```
src/
├── components/      # UI-компоненты (Board, Column, Task, AddTask)
├── hooks/           # Кастомные хуки (useBoard, useLocalStorage)
├── shared/          # Хелперы
├── constants/       # Константы (статусы и т.п.)
├── App.jsx          # Тонкий container-компонент
└── main.jsx         # Точка входа
```

---

## 🔧 Как работает приложение

### Архитектура

Проект построен с разделением ответственности:

- `components` — только UI  
- `useBoard` — бизнес-логика доски (CRUD, drag & drop, статусы)  
- `useLocalStorage` — работа с browser storage  
- helpers / constants — доменные вспомогательные функции  

`App.jsx` выступает как container-компонент и не содержит бизнес-логики.

---

### Управление состоянием

Состояние задач управляется внутри кастомного хука `useBoard`.

Каждая задача имеет структуру:

```js
{
  id: Date.now(),
  title: "Task title",
  status: "ToDo"
}
```

Реализован полный CRUD:

- создание
- чтение (рендер)
- обновление
- удаление

Все изменения выполняются через функциональные обновления состояния.

---

### Сохранение данных

Для сохранения данных используется кастомный хук `useLocalStorage`.

**Принцип работы:**

1. При инициализации state данные читаются из localStorage  
2. При каждом изменении состояние автоматически сериализуется и сохраняется  
3. `useBoard` не знает о браузерном API — этим занимается `useLocalStorage`

Такой подход позволяет легко заменить способ хранения данных в будущем.

---

## 👤 Автор

**Кирилл Васильев**

GitHub: https://github.com/vasilyefff
